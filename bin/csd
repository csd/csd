#!/usr/bin/env ruby

# First, load the Rubygems framework and the CSD gem.
# Note: Rubygems is already included for Ruby >= 1.9, but we want to go sure here
require 'rubygems'
require 'csd'

# Now, let's bootstrap the Command Line Interface and off we go
begin
  CSD.bootstrap
#rescue NameError
#  puts 'NameError! If you are an AI-developer, you might want to use `ruby bin/csd-devel´ instead.'
#  exit 1
#rescue CSD::BundlerError => e
#  CSD.ui.error e.message
#  exit e.status_code
#rescue Interrupt
#  CSD.ui.error "\nQuitting..."
#  exit 1
end